# 安装文档

一 基础环境安装（1.vite）

```
//创建vite环境
yarn create vite my-vue --template vue 
//安装基本配置
cnpm install mockjs -S
cnpm install js-cookie -S
yarn add js-cookie -S
yarn add @element-plus/icons-vue -S
yarn add element-plus -S
yarn add vue-router -S   //路由跳转
yarn add axios -S 		 //二次跳转封装
yarn add less less-loader -S
yarn add echarts -S
yarn add echarts@4.9 -S  //@用来指定版本

//轮播图的环境 --使用@也得安装它
cnpm install -D unplugin-vue-components unplugin-auto-import -S

//打包问题
cnpm install --save-dev html-webpack-inline-source-plugin
cnpm install --save-dev html-webpack-plugin.

yarn create vite beifen --template vue 
```

解释：

```
yarn add element-plus -S  
```



```
yarn add element-plus -S  
//安装element-plus环境

//还需要在main.js中配置环境
import { createApp } from 'vue'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import App from './App.vue'

const app = createApp(App)

app.use(ElementPlus)
app.mount('#app')
```

```
yarn add @element-plus/icons-vue -S  //element-plus图标的安装
```

```
//还需要全局引用，在main.js中加入
import * as ElementPlusIconsVue from '@element-plus/icons-vue'

const app = createApp(App)
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)
}
```





解决@的问题：

```
npm install vue@next
//先安装环境

//修改vite.config.js文件内容：

import { fileURLToPath, URL } from "node:url";
import { defineConfig } from "vite";
import vue from "@vitejs/plugin-vue";

import AutoImport from 'unplugin-auto-import/vite';
import Components from 'unplugin-vue-components/vite';
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers';

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      resolvers: [ElementPlusResolver()],
    }),
    Components({
      resolvers: [ElementPlusResolver()],
    }),
  ],

  resolve: {
    alias: {
      "@": fileURLToPath(new URL("./src", import.meta.url)),
    },
  },

  server: {
    port: 8080,
  },
});

```

```
<template>
  <div class="dengluye">
    <div v-for="row in 4" :key="row" class="login-row">
      <div v-for="column in 4" :key="getColumnIndex(row, column)" class="login-panel-container">
        <div class="login-panel" v-if="loginButtonNames[getColumnIndex(row, column)]">
          <div class="row">
            <button  class="denglu" @click="login(getColumnIndex(row, column))">{{ loginButtonNames[getColumnIndex(row, column)].name }}</button>
          </div>
          <div class="row">
              <div class="input-with-icon">
                  <el-icon name="el-icon-user" class="icon"><ChromeFilled /></el-icon>
                  <input class="url" type="text" v-model="loginButtonNames[getColumnIndex(row, column)].url" placeholder="URL链接" />
              </div>
          </div>
          <div class="row">
              <div class="input-with-icon">
                  <el-icon name="el-icon-user" class="icon"><UserFilled /></el-icon>
                  <input type="text" v-model="loginButtonNames[getColumnIndex(row, column)].account" placeholder="账号输入" />
              </div>
          </div>
          <div class="row">
              <div class="input-with-icon">
                  <el-icon class="icon"><Lock /></el-icon>
                  <input type="text" v-model="loginButtonNames[getColumnIndex(row, column)].password" placeholder="密码输入" />
              </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
export default {
  data() {
      return {
      loginButtonNames: {
          1: {
          name: '宜宾烟草',
          url: 'tobacco-test.spacemv.com',
          account: 'test1',
          password: '1234,abcd'
          },
          2: {
          name: '常州数字农业',
          url: 'cz-agriculture.spacemv.com',
          account: 'admin',
          password: '123456'
          },
          3: {
          name: '川西北碳中和',
          url: 'ecology-demo.spacemv.com',
          account: 'test@spacemv.com',
          password: '123123123'
          },
          4: {
          name: '通信线路预警-西昌管线',
          url: 'xc-line.txxw.com:8200',
          account: 'null',
          password: 'null'
          },
          5: {
          name: '重庆地铁沉降监测',
          url: 'sink.spacemv.com',
          account: 'test@spacemv.com',
          password: '123123123'
          },
          6: {
          name: '太空机器视觉-解译大脑展示',
          url: 'http://spacemv.txxw.com/platform',
          account: 'tx@tianxunxingwang',
          password: '123123123'
          },
          7: {
          name: '理县民兵便携式指挥平台/应急指挥沙盘',
          url: 'http://militia.spacemv.com',
          account: 'a@f.com',
          password: '123456'
          },
          8: {
          name: '交通部全国道路货运车辆监管系统',
          url: 'https://www.gghypt.net',
          account: 'U-Key用户登录->祁飞',
          password: 'we85gc61'
          },
          9: {
          name: '运行场景可视化推演及性能分析平台',
          url: 'http://gislink.zoogooy.com',
          account: 'null',
          password: 'null'
          },
          10: {
          name: '太空机器视觉-目标态势展示',
          url: 'http://spacemv.txxw.com/platform',
          account: 'tx@tianxunxingwang',
          password: '123123123'
          },
          11: {
          name: '航天任务分析平台',
          url: 'http://139.224.107.180/9100',
          account: 'TX',
          password: 'TX123'
          },
          12: {
          name: '伪装效果评估',
          url: '127.0.0.1:1234',
          account: 'null',
          password: 'null'
          },
          13: {
          name: '太原二期后台',
          url: 'localhost:10050/api/swagger-ui.html',
          account: 'null',
          password: 'null'
          },
          14: {
          name: '601电磁频谱后台',
          url: 'spectrum-dev.txxw.com/api/swagger-ui.html',
          account: 'null',
          password: 'null'
          },
          15: {
          name: '',
          url: '',
          account: '',
          password: ''
          },
          16: {
          name: '',
          url: '',
          account: '',
          password: ''
          },
          // 可以根据需要继续添加其他登录按钮名称及对应的URL、账号和密码
      },

  };
  },
  methods: {
      getColumnIndex(row, column) {
      return (row - 1) * 4 + column;
      },
      login(index) {
      const { name, url, account, password } = this.loginButtonNames[index];
      // 在这里执行登录逻辑，可以调用后端 API 进行验证等操作
      console.log(`第 ${index} 个登录界面：`);
      console.log('登录名:', name);
      console.log('URL链接:', url);
      console.log('账号输入:', account);
      console.log('密码输入:', password);

      window.open(url);
      },
  }
};
</script>

<style>
.login-row{
  margin-left: 40px;
}
.login-panel-container {
  border: 1px solid #ccc;
  padding: 10px;
  margin-top: 35px;
  margin-left: 23px;
  width: 23%;
  display: inline-block;
  border: 2px solid #000;
  border-radius: 20px;
  background-color: rgba(92, 91, 111, 0.64);


}
.denglu{
  background-color: #1890ff;
  height: 37px;
  width: 380px;
  margin-left: 15px;
  opacity: 0.9;
  border-radius: 10px;
  color: #fff;
  font-size: 18px;
}
.row {
margin-bottom: 10px;
}
.login-panel{
  height: 200px;
  width: 350px;

}
.input-with-icon {
  color: #1890ff;

  margin-top: 10px;
  position: relative;
  margin-left: 15px;
  /* width: 377px; */
  /* background-color: #1890ff; */
  margin: 10px;
}

.icon {
position: absolute;
left: 10px;
top: 50%;
transform: translateY(-50%);
color: #ccc;

}


input[type="text"],
input[type="password"] {
/* padding-left: 100px;  */
width: 345px;
height: 32px;
margin-left: 5px;
/* opacity: 0.2;
background-color: #fff; */
background-color: rgba(255, 255, 255, 0.2);
font-size: 20px;
/* color: red; */
border: 1px solid #fff;
border-radius: 10px;
padding-left: 30px;
color: #fff;
}
.eye-icon {
position: absolute;
top: 50%;
right: 10px;
transform: translate(-50%, -50%);
cursor: pointer;
}

.eye-open {
color: #1890ff; /* 设置打开密码可见状态时的颜色 */
}
</style>

```

```
//地图代码备份

<template>
    <!-- <div class="content row-flex-start" style="min-width:1500px; ovorflow-x:auto"> -->
    <div class="content row-flex-start" style="min-width:1500px;">    
      <div class="left_map" id="left_map"  @click="showChinaMap"></div>
      <div class="right_opetate row-center" id="right_opetate">
      </div>
    </div>
  </template>
  
  <script>
  import * as echarts from "echarts";
  import china from 'echarts/map/json/china.json'
  import axios from 'axios'
  echarts.registerMap('china', china)
    var provinces = ['shanghai', 'hebei', 'shanxi', 'neimenggu', 'liaoning', 'jilin', 'heilongjiang', 'jiangsu', 'zhejiang', 'anhui', 'fujian', 'jiangxi', 'shandong', 'henan', 'hubei', 'hunan', 'guangdong', 'guangxi', 'hainan', 'sichuan', 'guizhou', 'yunnan', 'xizang', 'shanxi1', 'gansu', 'qinghai', 'ningxia', 'xinjiang', 'beijing', 'tianjin', 'chongqing', 'xianggang', 'aomen', 'taiwan']
    var provincesText = ['上海', '河北', '山西', '内蒙古', '辽宁', '吉林', '黑龙江', '江苏', '浙江', '安徽', '福建', '江西', '山东', '河南', '湖北', '湖南', '广东', '广西', '海南', '四川', '贵州', '云南', '西藏', '陕西', '甘肃', '青海', '宁夏', '新疆', '北京', '天津', '重庆', '香港', '澳门', '台湾']
  export default {
    created () {
    },
    mounted () {
      this.$nextTick(() => {
        this.initMap()
      })
    },
    data () {
      return {
        map: {}
      }
    },
    props: {},
    methods: {
      getMapOpt (place) {
        const option = {
        backgroundColor: 'transparent', //地图背景颜色
        //以下是地图标题，我在此处设空，需要的可以自己加上
        // title: {
        //     text: '中国地图',
        //     // subtext: '1',
        //     left: 'center'
        //   },
          //以下是地图右侧“还原”“下载”工具框
          toolbox: {
              show: true,
              orient: 'vertical',
              left: 'right',
              top: 'center',
              feature: {
                  restore: {},
                  saveAsImage: {}
              }
          },
        geo: {
          map: place || 'china',
          label: {
              emphasis: {
                  show: false
              },
              // 不需要显示地名可直接删除normal此项
              normal: {
                show: true, // 是否显示对应地名
                textStyle: {
                    color: 'rgba(0,0,0)'
                }
              }
          },
          roam: true,
          itemStyle: {
              normal: {
                  areaColor: ['#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#6FA7CE','#fff','#6FA7CE','#fff','#6FA7CE','#fff','#6FA7CE','#fff','#6FA7CE','#fff','#6FA7CE','#fff','#6FA7CE','#fff','#6FA7CE','#fff','#6FA7CE','#fff','#6FA7CE'], //地图颜色
                  borderColor: '#fff' //地图边线颜色
              },
              emphasis: {
                  areaColor: 'red' //鼠标移入颜色
              }
          }
         }
        }
        return option
      },
      // 显示中国地图
      showChinaMap () {
         const option = this.getMapOpt()
          this.map.setOption(option, true)
      },
      // 显示各省地图，这里使用axios请求本地文件，provice文件夹存在的位置为public/province（旧版本是static）
      getProvinceMapOpt (provinceAlphabet) {
        axios.get('province/' + provinceAlphabet + '.json').then((s) => {
          echarts.registerMap(provinceAlphabet, s.data)
          const option = this.getMapOpt(provinceAlphabet)
          this.map.setOption(option, true)
        })
      },
      initMap () {
        var dom = document.getElementById('left_map')
        dom.style.width = '700px';
        dom.style.height = '600px';
        // dom.style.marginLeft='-10px';
        dom.style.marginTop='-80px';
        // dom.style.marginBlock='-100px';

        this.map = echarts.init(dom)
        const option = this.getMapOpt()
        if (option && typeof option === 'object') {
          this.map.setOption(option, true)
        }
        this.map.on('click', (param) => {
          // console.log(param)
          event.stopPropagation()// 阻止冒泡
          // 找到省份名
          const provinceIndex = provincesText.findIndex(x => {
            return param.name === x
          })
          //在这里判断provincesText中是否包含点击的省份名，有则渲染省级地图，无则无处理（需要可从此获得）
          if (provinceIndex === -1) return
          const provinceAlphabet = provinces[provinceIndex]
          // 重新渲染各省份地图
          this.getProvinceMapOpt(provinceAlphabet)
        })
      }
    },
    computed: {}
  }
  </script>
  
  <style>
.content row-flex-start{
  margin-top: 20px;
}
  .left_map {
    width: 600px; 
    height: 300px; 
    overflow: hidden;
    min-width: 40%;
  }
  .right_opetate {
    flex: 1;
    height: 100%;
    width: 100vw;
    background: #031234;
    overflow: hidden;
    min-width: 40%;


  }
  </style>
  
  
```

```
<template>
    <div class="overall">
        <el-main>
            <el-row>
                <el-col class='main-l' :span="7">
                    <div class="graph">
                            <el-card class="custom-card" style="height: 380px; width: 100vw; border: none;">
                                <div class="zhu-title"><h2>点击量统计</h2></div>
                                <div ref="userechart" style="height: 370px;"></div>
                            </el-card>
                    </div>
                    <div class="main-commpon">
                        <el-card style="height: 440px; border: none;" >
                                <el-card style="height: 60px; background-color: #122d69;
                                margin-top: 0px; display: flex; justify-content: center; align-items: center;border: none;">

                                <h1 class="flowing-text">访问入口</h1>
                                </el-card>
                                <div >
                                    <el-scrollbar height="360px">
                                        <el-card  v-for="item in zongProject" 
                                        :key="item" class="scrollbar-demo-item" @click="goToUrl(item.url)">
                                        <p class="name-tim">{{item.name}}</p>
                                        </el-card>
                                    </el-scrollbar>
                                </div>
                        </el-card>
                    </div>
                </el-col>
                <el-col class='main-m' :span="10">
                    <div class="map-m" >
                        <common-map></common-map>
                    </div>
                    <div class="lunbo-div">
                        <!-- <el-card class="lunboka"> -->
                        <el-carousel indicator-position="outside" class="lunbo">
                            <el-carousel-item v-for="item in carouselData" :key="item">
                            <p class="lunbo-name">{{ item.name }}</p> <!-- 修改这里，用 <p> 标签包裹展示的文字 -->
                            <p class="lunbo-data">{{ item.data }}</p>
                            </el-carousel-item>
                        </el-carousel>
                    <!-- </el-card> -->
                    </div>


                    <div class="project1">
                        <el-card :body-style="{display:'flex', padding:0,}" v-for="item in projectData"
                            :key="item.province" :class="{'left-align':index >= 4}" @click="handleCardClick(item)">
                                <div class="details1">
                                    <p class="num1" style="cursor: pointer;">{{ item.name }}</p>
                                </div>                   
                        </el-card>
                    </div>
                    <div class="scroll-container" >
                            <el-card :body-style="{display:'flex', padding:0,}" v-for="item in countData"
                            :key="item.province" :class="{'left-align':index >= 5}">
                                <div class="details">
                                    <p class="num">{{ item.province }}</p>
                                    <p class="txt">{{ item.count }}</p>
                                </div>                   
                            </el-card>
                    </div>                   

                </el-col>
                <el-col class='main-r' :span="7">
                    <div>
                        <div class="zhexian">
                            <el-card style = "height:380px; border: none;" >
                                <div class="zhexian-title"><h2>盈利趋势</h2></div>
                                <div ref="echart" style = "height:370px"></div>
                            </el-card>
                        </div>

                        <div class="bing">
                            <el-card style="height: 410px; border: none;">
                                <div class="bing-title"><h2>合作企业</h2></div>
                                <div ref="videoechart" style="height: 380px"></div>
                            </el-card>
                        </div>
                    </div>
                </el-col>
            </el-row>
        </el-main>
    </div>
</template>

<script>
import {
  defineComponent,
  getCurrentInstance,
  onMounted,
  reactive,
  ref,
} from "vue";
import * as echarts from "echarts";
import axios from 'axios';
export default defineComponent({
    data() {
        return {
            selectedButton:'',
            
            carouselData: [
                {
                    "name":"智慧物流",
                    "data":"智慧物流是以信息技术为支撑，在物流运输、仓库、包装、装卸搬运、流通加工、配送、信息服务等各个阶段实现系统感知的物流模式。",
                },
                {
                    "name":"伪装目标",
                    "data":"伪装技术，实现作战力量、作战行动的全过程伪装，力求实现技术伪装与行动伪装的有机统一。",
                },
                {
                    "name":"数字交通",
                    "data":"数字交通是数据闭环赋能体系，通过数据全域标识、状态精准感知、数据实时分析、模型科学决策、智能精准执行，实现交通的模拟、监控、诊断、预测和控制。",
                },
                {
                    "name":"常州农业",
                    "data":"常州农业是将信息作为农业生产要素，用现代信息技术对农业对象  环境和全过程进行可视化表达、数字化设计、信息化管理的现代农业。",
                },
            ],
        };
    },
    methods:{
        goToUrl(url){
            window.open(url,'_blank')
        },
    },
    setup(){

        let getImageUrl = (user) => {
        return new URL(`../assets/images/${user}.png`, import.meta.url).href;
        };
        const { proxy} = getCurrentInstance();
        let projectData = ref({});
        let countData = ref([]);
        let zongProject = ref([]);
        // let getcooperative = ref([]);
        //获取省份数据：//目前是十个省份
        const getCountData = async () => {
            let res = await proxy.$api.getCountData();
            // console.log(res);
            countData.value = res;
        };
        //首页的项目入口数据
        const getZongProject = async () => {
            let res = await proxy.$api.getZongProject();
            // console.log(res);
            zongProject.value = res;
        };
        //地图下面的项目
        const getProject = async () => {
            let res = await proxy.$api.getProject();
            // console.log(res);
            projectData.value = res;
        };

        // const getCooperative = async () => {
        //     let res = await proxy.$api.getCooperative();
        //     getcooperative.value = res;
        // };
        onMounted(()=>{
            getChartData();
            getCountData();
            getZongProject();
            getProject();
            // getCooperative();
        })
        //关于echarts 表格的渲染部分
        let xOptions = reactive({
            // 图例文字颜色
            textStyle: {
                color: "#fff",
            },
            grid: {
                left: "20%",
            },
            // 提示框
            tooltip: {
                trigger: "axis",
            },
            xAxis: {
                type: "category", // 类目轴
                data: [],
                axisLine: {
                lineStyle: {
                    color: "#17b3a3",
                },
                },
                axisLabel: {
                interval: 0,
                color: "#fff",
                },
            },
            yAxis: [
                {
                type: "value",
                axisLine: {
                    lineStyle: {
                    color: "#17b3a3",
                    },
                },
                },
            ],
            color: ["#2ec7c9", "#b6a2de", "#5ab1ef", "#ffb980", "#d87a80", "#8d98b3"],
            series: [],
        });
        let pieOptions = reactive({
            tooltip: {
                trigger: "item",
            },
            color: [
                "#0f78f4",
                "#dd536b",
                "#9462e5",
                "#a6a6a6",
                "#e1bb22",
                "#39c362",
                "#3ed1cf",
            ],
            series: [],
        });
        let orderData = reactive({
            xData: [],
            series: [],
            });
        let userData = reactive({
            xData: [],
            series: [],
        });
        let videoData = reactive({
            series: [],
        });


    //获取数据
        const getChartData = async()=>{
            let result = await proxy.$api.getChartData();
            // console.log(result);
            let res = result.orderData;
            let userRes = result.userData;
            // console.log(userRes);
            let videoRes = result.videoData;
            orderData.xData = res.date;  //x
            const keyArray = Object.keys(res.data[0]);
            const series = [];
            // keyArray.forEach((key) => {
            //   series.push({
            //     name: key,
            //     data: res.data.map((item) => item[key]),
            //     type: "line",
            //     });
            // });
            res.data.forEach((item) => {
                const brandName = item.name;
                const salesData = item.data;
                series.push({
                    name: brandName,
                    data: salesData,
                    type: "line",
                });
            });
            orderData.series = series;
            xOptions.xAxis.data = orderData.xData;
            xOptions.series = orderData.series;        
                        //进行渲染
            let hEcharts = echarts.init(proxy.$refs["echart"]);
            hEcharts.setOption(xOptions);



            //柱状图绘制  
            userData.xData = userRes.date;  //x
            const zhukeyArray = Object.keys(userRes.data[0]);
            const zhuseries = [];

            userRes.data.forEach((item) => {
                const zhubrandName = item.name;
                const zhusalesData = item.data;
                zhuseries.push({
                    name: zhubrandName,
                    data: zhusalesData,
                    type: "bar",
                });
            });
            userData.series = zhuseries;
            xOptions.xAxis.data = userData.xData;
            xOptions.series = userData.series;  
                  
                        //进行渲染
            let uEcharts = echarts.init(proxy.$refs["userechart"]);
            uEcharts.setOption(xOptions);

  
           //饼形图绘制 
            videoData.series = [
                {
                data: videoRes,
                type: "pie",
                },
            ];
            pieOptions.series = videoData.series;
            let vEcharts = echarts.init(proxy.$refs["videoechart"]);
            vEcharts.setOption(pieOptions);
        };
        return {
            getImageUrl,
            countData,
            zongProject,
            projectData,
            // getcooperative,
            // tableData,
        }

    },
});
</script>

<style scoped>

.main-l {
    border: 0px solid transparent; /* 添加边框样式 */
    padding: 19px; /* 添加内边距 */
    width: 40vw;
    /* background-color: #031234; */
    color: #fff;
    font-size: 30px;
}

.main-m {
    border: 0px solid transparent; /* 添加边框样式 */
    padding: 19px; /* 添加内边距 */
    width: 40vw;
    /* background-color: #031234; */
    color: #fff;
    font-size: 30px;
    overflow: hidden;
    margin-top: 20px;
    background-color: transparent;
    border-radius: 20px;

}

.lunbo{
    width: 10vw;
    height: 10vw;
    margin-top: -150px;
    background-color: transparent;


}
.lunbo-name{
    font-size: 25px;
    color: #169bd5;
    border-bottom: 4px solid #169bd5; /* 将 #000 替换为您想要的分隔线的颜色 */
    margin-bottom: 10px; /* 可根据需要调整分隔线下方的间距 */
    margin-left: 10px;

}
.lunbo-data{
    font-size: 18px;   
    color: #00a65b;

}


.map-m{
    min-width: 40%;
}
.main-r {
    
    /* border: 0px solid #000;  */
    padding: 19px; /* 添加内边距 */
    width: 40vw;
    /* background-color: #031234; */
    color: transparent;
    font-size: 30px;
    border: 0px solid transparent; /* 添加边框样式 */


}
/* 柱状图 */
.graph{
    /* margin-top: 20px; */
    border: 0px solid transparent; /* 添加边框样式 */

    display: flex;
    justify-content: space-between;
    .el-card{
        width:100%;
        background-color: transparent;
    }
}
.zhu-title h2 {
    /* border: 1px solid transparent; */
    background-image: -webkit-linear-gradient(90deg, #ff0000, #00ff00); /* WebKit 浏览器的渐变色 */
    -webkit-background-clip: text; /* WebKit 浏览器的文本裁剪 */
    background-clip: text; /* 标准的文本裁剪 */
    -webkit-text-fill-color: transparent; /* WebKit 浏览器的文字颜色设置为透明 */
    color: #fff;
    font-size: 20px;
    /* animation: flowingAnimation 5s infinite linear; */
}

.zhexian{
    border: 0px solid transparent; /* 添加边框样式 */
    /* margin-top: 20px; */
    display: flex;
    justify-content: space-between;
    .el-card{
        /* margin-top: -20px; */
        width:100%;
        background-color: transparent;
    }

}
.zhexian-title h2 {
    /* border: 1px solid transparent; */
    background-image: -webkit-linear-gradient(90deg, #ff0000, #00ff00); /* WebKit 浏览器的渐变色 */
    -webkit-background-clip: text; /* WebKit 浏览器的文本裁剪 */
    background-clip: text; /* 标准的文本裁剪 */
    -webkit-text-fill-color: transparent; /* WebKit 浏览器的文字颜色设置为透明 */
  color: #fff;
  font-size: 20px;
}
.bing{
    border: 0px solid transparent; /* 添加边框样式 */
    margin-top: 20px;
    display: flex;
    justify-content: space-between;
    .el-card{
        width:100%;
        background-color: transparent;
    }
}
.bing-title h2 {
  /* 添加渐变色的边框样式 */
  /* border: 1px solid transparent; */
  background-image: -webkit-linear-gradient(90deg, #ff0000, #00ff00); /* WebKit 浏览器的渐变色 */
  -webkit-background-clip: text; /* WebKit 浏览器的文本裁剪 */
  background-clip: text; /* 标准的文本裁剪 */
  -webkit-text-fill-color: transparent; /* WebKit 浏览器的文字颜色设置为透明 */
  color: #fff;
  font-size: 20px;
}
.project1{
    /* width: 560px; */
    margin-top: 40px;
    border: 0px solid transparent; 
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-start;
    /* justify-content: center; */
    align-items: center;
    height: 45px; 
    overflow: auto;

    .el-card{
        width: 23%;
        margin: 5px;
        background-color: #122d69;
        color: #ff0000;
        display: flex;
        /* align-items: center; */
        /* margin: auto */
    }; 
}
.num1{
    text-align: center;
    margin-left: 20px;
}
.project1::-webkit-scrollbar {
  width: 8px;
  background-color: transparent; /* 设置滚动条背景颜色，根据需要调整 */
}
.scroll-container {
    /* width: 560px; */
    text-align:center;
    margin-top: 30px;
    border: 0px solid transparent; /* 添加边框样式 */
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-start;
    height: 102px; /* 设置容器高度 */
    overflow: auto; /* 添加滚动条 */
    .el-card{
        width: 19%;
        margin: 1px;
        background-color: transparent;
        border: 1px solid #122d69; /* 添加边框样式 */
    };
    .details{
        margin-left: 15px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        text-align:center;
        margin-left: 25%;


        .num{
            font-size: 19px;
            text-align:center;
            font-weight: bold;
            margin-bottom: 8px;
            color: #ff0000;
        }
        .txt{
            font-size: 20px;
            text-align:center;
            color: #ff0000;
        }
    }
}
/* .scroll-container.left-align{
    justify-content: flex-start;
} */
.scroll-container::-webkit-scrollbar {
  width: 8px;
  background-color: transparent; /* 设置滚动条背景颜色，根据需要调整 */
}

.scrollbar-demo-item {
  border: 0px solid transparent;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 50px;
  margin: 10px;
  text-align: center;
  border-radius: 4px;
  background: var(--el-color-primary-light-9);
  color: var(--el-color-primary);
  background: transparent;
}
.name-tim{
    cursor: pointer;

}

.main-commpon{
    border: 0px solid transparent;
    .el-card{
        background-color: transparent;
    }
}

.flowing-text {
  font-weight: bold;
  font-size: 32px;
  position: relative;
  display: inline-block;
  animation: flowingAnimation 5s infinite linear;
}
@keyframes flowingAnimation {
  0% { color: #ff0000; }
  14% { color: #ff7f00; }
  29% { color: #ffff00; }
  43% { color: #00ff00; }
  57% { color: #0000ff; }
  71% { color: #8b00ff; }
  86% { color: #ffc0cb; }
  100% { color: #ff0000; }
}
/* .el-carousel__item p {
  display: flex;
  opacity: 0.75;
  margin: 0;
  color: #ff0000;

} */
.el-carousel__item:nth-child(2n) {
    
  /* background-color: #99a9bf; */
  background-color: transparent;
  border: 1px solid transparent; /* 添加边框样式 */
  height: 190px;
}
.el-carousel__item:nth-child(2n + 1) {
  background-color: transparent;
  border: 1px solid transparent; /* 添加边框样式 */
  height: 190px;
}
</style>
<CommonMap />
```





```
<template>
  <div class="overall">
      <el-main>
          <el-row>
              <el-col class='main-l' :span="7">
                  <div class="graph">
                          <el-card class="custom-card" style="height: 380px; width: 100vw; border: none;">
                              <div class="zhu-title"><h2>点击量统计</h2></div>
                              <div ref="userechart" style="height: 370px;"></div>
                          </el-card>
                  </div>
                  <div class="main-commpon">
                      <el-card style="height: 440px; border: none;" >
                              <el-card style="height: 60px; background-color: #122d69;
                              margin-top: 0px; display: flex; justify-content: center; align-items: center;border: none;">

                              <h1 class="flowing-text">访问入口</h1>
                              </el-card>
                              <div >
                                  <el-scrollbar height="360px">
                                      <el-card  v-for="item in zongProject" 
                                      :key="item" class="scrollbar-demo-item" @click="goToUrl(item.url)">
                                      <p class="name-tim">{{item.name}}</p>
                                      </el-card>
                                  </el-scrollbar>
                              </div>
                      </el-card>
                  </div>
              </el-col>
              <el-col class='main-m' :span="10">
                  <div class="map-m" >
                      <common-map></common-map>
                  </div>
                  <div class="lunbo-div">
                      <!-- <el-card class="lunboka"> -->
                      <el-carousel indicator-position="outside" class="lunbo">
                          <el-carousel-item v-for="item in carouselData" :key="item">
                          <p class="lunbo-name">{{ item.name }}</p> <!-- 修改这里，用 <p> 标签包裹展示的文字 -->
                          <p class="lunbo-data">{{ item.data }}</p>
                          </el-carousel-item>
                      </el-carousel>
                  <!-- </el-card> -->
                  </div>

                  <div class="project1">
                    <el-card :body-style="{display:'flex', padding:0,}" :class="{'left-align':index >= 4}">
                      <div class= "details1">
                        <button class="num1" @click="loadZongProject">常州农业</button>
                        <button @click="loadCooperative">智慧物流</button>
                        <button @click="loadZongProject">伪装项目</button>
                        <button @click="loadCooperative">数字交通</button>
                      </div>
                    </el-card>       
                  </div>

                  <div class="scroll-container" >
                          <el-card :body-style="{display:'flex', padding:0,}" v-for="item in countData"
                          :key="item.province" :class="{'left-align':index >= 5}">
                              <div class="details">
                                  <p class="num">{{ item.province }}</p>
                                  <p class="txt">{{ item.count }}</p>
                              </div>                   
                          </el-card>
                  </div>                   

              </el-col>
              <el-col class='main-r' :span="7">
                  <div>
                      <div class="zhexian">
                          <el-card style = "height:380px; border: none;" >
                              <div class="zhexian-title"><h2>盈利趋势</h2></div>
                              <div ref="echart" style = "height:370px"></div>
                          </el-card>
                      </div>

                      <div class="bing">
                          <el-card style="height: 410px; border: none;">
                              <div class="bing-title"><h2>合作企业</h2></div>
                              <div ref="videoechart" style="height: 380px"></div>
                          </el-card>
                      </div>
                  </div>
              </el-col>
          </el-row>
      </el-main>
  </div>
</template>

<script>
import {
defineComponent,
getCurrentInstance,
onMounted,
reactive,
ref,
} from "vue";
import * as echarts from "echarts";
import axios from 'axios';
export default defineComponent({
  data() {
      return {
          selectedButton:'',

          carouselData: [
              {
                  "name":"智慧物流",
                  "data":"智慧物流是以信息技术为支撑，在物流运输、仓库、包装、装卸搬运、流通加工、配送、信息服务等各个阶段实现系统感知的物流模式。",
              },
              {
                  "name":"伪装目标",
                  "data":"伪装技术，实现作战力量、作战行动的全过程伪装，力求实现技术伪装与行动伪装的有机统一。",
              },
              {
                  "name":"数字交通",
                  "data":"数字交通是数据闭环赋能体系，通过数据全域标识、状态精准感知、数据实时分析、模型科学决策、智能精准执行，实现交通的模拟、监控、诊断、预测和控制。",
              },
              {
                  "name":"常州农业",
                  "data":"常州农业是将信息作为农业生产要素，用现代信息技术对农业对象  环境和全过程进行可视化表达、数字化设计、信息化管理的现代农业。",
              },
          ],
      };
  },
  methods:{
      goToUrl(url){
          window.open(url,'_blank')
      },
  },
  setup(){

      let getImageUrl = (user) => {
      return new URL(`../assets/images/${user}.png`, import.meta.url).href;
      };
      const { proxy} = getCurrentInstance();
      let projectData = ref({});
      let countData = ref([]);
      let zongProject = ref([]);
      // let getcooperative = ref([]);
      //获取省份数据：//目前是十个省份
      const getCountData = async () => {
          let res = await proxy.$api.getCountData();
          // console.log(res);
          countData.value = res;
      };
      //首页的项目入口数据
      const getZongProject = async () => {
          let res = await proxy.$api.getZongProject();
          // console.log(res);
          zongProject.value = res;
      };
      //地图下面的项目
      const getProject = async () => {
          let res = await proxy.$api.getProject();
          // console.log(res);
          projectData.value = res;
      };

      // const getCooperative = async () => {
      //     let res = await proxy.$api.getCooperative();
      //     getcooperative.value = res;
      // };
      onMounted(()=>{
          getChartData();
          getCountData();
          getZongProject();
          getProject();
          // getCooperative();
      })
      //关于echarts 表格的渲染部分
      let xOptions = reactive({
          // 图例文字颜色
          textStyle: {
              color: "#fff",
          },
          grid: {
              left: "20%",
          },
          // 提示框
          tooltip: {
              trigger: "axis",
          },
          xAxis: {
              type: "category", // 类目轴
              data: [],
              axisLine: {
              lineStyle: {
                  color: "#17b3a3",
              },
              },
              axisLabel: {
              interval: 0,
              color: "#fff",
              },
          },
          yAxis: [
              {
              type: "value",
              axisLine: {
                  lineStyle: {
                  color: "#17b3a3",
                  },
              },
              },
          ],
          color: ["#2ec7c9", "#b6a2de", "#5ab1ef", "#ffb980", "#d87a80", "#8d98b3"],
          series: [],
      });
      let pieOptions = reactive({
          tooltip: {
              trigger: "item",
          },
          color: [
              "#0f78f4",
              "#dd536b",
              "#9462e5",
              "#a6a6a6",
              "#e1bb22",
              "#39c362",
              "#3ed1cf",
          ],
          series: [],
      });
      let orderData = reactive({
          xData: [],
          series: [],
          });
      let userData = reactive({
          xData: [],
          series: [],
      });
      let videoData = reactive({
          series: [],
      });


  //获取数据
      const getChartData = async()=>{
          let result = await proxy.$api.getChartData();
          
          // console.log(result);
          let res = result.orderData;
          let userRes = result.userData;
          // console.log(userRes);
          let videoRes = result.videoData;
          orderData.xData = res.date;  //x
          const keyArray = Object.keys(res.data[0]);
          const series = [];
          // keyArray.forEach((key) => {
          //   series.push({
          //     name: key,
          //     data: res.data.map((item) => item[key]),
          //     type: "line",
          //     });
          // });
          res.data.forEach((item) => {
              const brandName = item.name;
              const salesData = item.data;
              series.push({
                  name: brandName,
                  data: salesData,
                  type: "line",
              });
          });
          orderData.series = series;
          xOptions.xAxis.data = orderData.xData;
          xOptions.series = orderData.series;        
                      //进行渲染
          let hEcharts = echarts.init(proxy.$refs["echart"]);
          hEcharts.setOption(xOptions);



          //柱状图绘制  
          userData.xData = userRes.date;  //x
          const zhukeyArray = Object.keys(userRes.data[0]);
          const zhuseries = [];

          userRes.data.forEach((item) => {
              const zhubrandName = item.name;
              const zhusalesData = item.data;
              zhuseries.push({
                  name: zhubrandName,
                  data: zhusalesData,
                  type: "bar",
              });
          });
          userData.series = zhuseries;
          xOptions.xAxis.data = userData.xData;
          xOptions.series = userData.series;  
                
                      //进行渲染
          let uEcharts = echarts.init(proxy.$refs["userechart"]);
          uEcharts.setOption(xOptions);


         //饼形图绘制 
          videoData.series = [
              {
              data: videoRes,
              type: "pie",
              },
          ];
          pieOptions.series = videoData.series;
          let vEcharts = echarts.init(proxy.$refs["videoechart"]);
          vEcharts.setOption(pieOptions);
      };
      return {
          getImageUrl,
          countData,
          zongProject,
          projectData,
          // getcooperative,
          // tableData,
      }

  },
});
</script>

<style scoped>

.main-l {
  border: 0px solid transparent; /* 添加边框样式 */
  padding: 19px; /* 添加内边距 */
  width: 40vw;
  /* background-color: #031234; */
  color: #fff;
  font-size: 30px;
}

.main-m {
  border: 0px solid transparent; /* 添加边框样式 */
  padding: 19px; /* 添加内边距 */
  width: 40vw;
  /* background-color: #031234; */
  color: #fff;
  font-size: 30px;
  overflow: hidden;
  margin-top: 20px;
  background-color: transparent;
  border-radius: 20px;

}

.lunbo{
  width: 10vw;
  height: 10vw;
  margin-top: -150px;
  background-color: transparent;


}
.lunbo-name{
  font-size: 25px;
  color: #169bd5;
  border-bottom: 4px solid #169bd5; /* 将 #000 替换为您想要的分隔线的颜色 */
  margin-bottom: 10px; /* 可根据需要调整分隔线下方的间距 */
  margin-left: 10px;

}
.lunbo-data{
  font-size: 18px;   
  color: #00a65b;

}


.map-m{
  min-width: 40%;
}
.main-r {
  
  /* border: 0px solid #000;  */
  padding: 19px; /* 添加内边距 */
  width: 40vw;
  /* background-color: #031234; */
  color: transparent;
  font-size: 30px;
  border: 0px solid transparent; /* 添加边框样式 */


}
/* 柱状图 */
.graph{
  /* margin-top: 20px; */
  border: 0px solid transparent; /* 添加边框样式 */

  display: flex;
  justify-content: space-between;
  .el-card{
      width:100%;
      background-color: transparent;
  }
}
.zhu-title h2 {
  /* border: 1px solid transparent; */
  background-image: -webkit-linear-gradient(90deg, #ff0000, #00ff00); /* WebKit 浏览器的渐变色 */
  -webkit-background-clip: text; /* WebKit 浏览器的文本裁剪 */
  background-clip: text; /* 标准的文本裁剪 */
  -webkit-text-fill-color: transparent; /* WebKit 浏览器的文字颜色设置为透明 */
  color: #fff;
  font-size: 20px;
  /* animation: flowingAnimation 5s infinite linear; */
}

.zhexian{
  border: 0px solid transparent; /* 添加边框样式 */
  /* margin-top: 20px; */
  display: flex;
  justify-content: space-between;
  .el-card{
      /* margin-top: -20px; */
      width:100%;
      background-color: transparent;
  }

}
.zhexian-title h2 {
  /* border: 1px solid transparent; */
  background-image: -webkit-linear-gradient(90deg, #ff0000, #00ff00); /* WebKit 浏览器的渐变色 */
  -webkit-background-clip: text; /* WebKit 浏览器的文本裁剪 */
  background-clip: text; /* 标准的文本裁剪 */
  -webkit-text-fill-color: transparent; /* WebKit 浏览器的文字颜色设置为透明 */
color: #fff;
font-size: 20px;
}
.bing{
  border: 0px solid transparent; /* 添加边框样式 */
  margin-top: 20px;
  display: flex;
  justify-content: space-between;
  .el-card{
      width:100%;
      background-color: transparent;
  }
}
.bing-title h2 {
/* 添加渐变色的边框样式 */
/* border: 1px solid transparent; */
background-image: -webkit-linear-gradient(90deg, #ff0000, #00ff00); /* WebKit 浏览器的渐变色 */
-webkit-background-clip: text; /* WebKit 浏览器的文本裁剪 */
background-clip: text; /* 标准的文本裁剪 */
-webkit-text-fill-color: transparent; /* WebKit 浏览器的文字颜色设置为透明 */
color: #fff;
font-size: 20px;
}
.project1{
  /* width: 560px; */
  margin-top: 40px;
  border: 0px solid transparent; 
  display: flex;
  flex-wrap: wrap;
  justify-content: flex-start;
  /* justify-content: center; */
  align-items: center;
  height: 45px; 
  overflow: auto;

  .el-card{
      width: 23%;
      margin: 5px;
      background-color: #122d69;
      color: #ff0000;
      display: flex;
      /* align-items: center; */
      /* margin: auto */
  }; 
}
.num1{
  text-align: center;
  margin-left: 20px;
}
.project1::-webkit-scrollbar {
width: 8px;
background-color: transparent; /* 设置滚动条背景颜色，根据需要调整 */
}
.scroll-container {
  /* width: 560px; */
  text-align:center;
  margin-top: 30px;
  border: 0px solid transparent; /* 添加边框样式 */
  display: flex;
  flex-wrap: wrap;
  justify-content: flex-start;
  height: 102px; /* 设置容器高度 */
  overflow: auto; /* 添加滚动条 */
  .el-card{
      width: 19%;
      margin: 1px;
      background-color: transparent;
      border: 1px solid #122d69; /* 添加边框样式 */
  };
  .details{
      margin-left: 15px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      text-align:center;
      margin-left: 25%;


      .num{
          font-size: 19px;
          text-align:center;
          font-weight: bold;
          margin-bottom: 8px;
          color: #ff0000;
      }
      .txt{
          font-size: 20px;
          text-align:center;
          color: #ff0000;
      }
  }
}
/* .scroll-container.left-align{
  justify-content: flex-start;
} */
.scroll-container::-webkit-scrollbar {
width: 8px;
background-color: transparent; /* 设置滚动条背景颜色，根据需要调整 */
}

.scrollbar-demo-item {
border: 0px solid transparent;
display: flex;
align-items: center;
justify-content: center;
height: 50px;
margin: 10px;
text-align: center;
border-radius: 4px;
background: var(--el-color-primary-light-9);
color: var(--el-color-primary);
background: transparent;
}
.name-tim{
  cursor: pointer;

}

.main-commpon{
  border: 0px solid transparent;
  .el-card{
      background-color: transparent;
  }
}

.flowing-text {
font-weight: bold;
font-size: 32px;
position: relative;
display: inline-block;
animation: flowingAnimation 5s infinite linear;
}
@keyframes flowingAnimation {
0% { color: #ff0000; }
14% { color: #ff7f00; }
29% { color: #ffff00; }
43% { color: #00ff00; }
57% { color: #0000ff; }
71% { color: #8b00ff; }
86% { color: #ffc0cb; }
100% { color: #ff0000; }
}
/* .el-carousel__item p {
display: flex;
opacity: 0.75;
margin: 0;
color: #ff0000;

} */
.el-carousel__item:nth-child(2n) {
  
/* background-color: #99a9bf; */
background-color: transparent;
border: 1px solid transparent; /* 添加边框样式 */
height: 190px;
}
.el-carousel__item:nth-child(2n + 1) {
background-color: transparent;
border: 1px solid transparent; /* 添加边框样式 */
height: 190px;
}
</style>
<CommonMap />




```





```


```

